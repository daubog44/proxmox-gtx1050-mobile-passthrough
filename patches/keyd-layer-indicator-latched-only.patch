diff --git a/src/daemon.c b/src/daemon.c
index 8eef21b..4d44800 100644
--- a/src/daemon.c
+++ b/src/daemon.c
@@ -143,7 +143,10 @@ static void on_layer_change(const struct keyboard *kbd, const struct layer *laye
 		int active_layers = 0;
 
 		for (i = 1; i < kbd->config.nr_layers; i++)
-			if (kbd->config.layers[i].type != LT_LAYOUT && kbd->layer_state[i].active) {
+			/*
+			 * Light Caps only for a persistent toggle, never for a held layer.
+			 */
+			if (kbd->config.layers[i].type != LT_LAYOUT && kbd->layer_state[i].toggled) {
 				active_layers = 1;
 				break;
 			}
